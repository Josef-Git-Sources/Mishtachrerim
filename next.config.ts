import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Image optimization: add remote domains here when course provider images are used
  // images: { remotePatterns: [] },
}

export default nextConfig
