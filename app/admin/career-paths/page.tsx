/**
 * Admin — Manage career paths — /admin/career-paths
 *
 * Create, edit, publish, and archive career_paths records.
 * Includes both career pages (page_type = 'career') and
 * intent pages (page_type = 'intent').
 *
 * Phase 1: placeholder.
 */
export default function AdminCareerPathsPage() {
  return (
    <section>
      <h1>Admin — Career Paths</h1>
      <p>placeholder</p>
    </section>
  )
}
